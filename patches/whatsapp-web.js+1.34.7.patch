diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index 540078a..078dfdc 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -345,11 +345,25 @@ exports.LoadUtils = () => {
                 let preview = await window
                     .require('WAWebLinkPreviewChatAction')
                     .getLinkPreview(link);
-                if (preview && preview.data) {
-                    preview = preview.data;
-                    preview.preview = true;
-                    preview.subtype = 'url';
-                    options = { ...options, ...preview };
+                if (preview) {
+                    let previewData = preview.data || preview;
+                    let plainPreview = {
+                        preview: true,
+                        subtype: 'url'
+                    };
+                    for (let key in previewData) {
+                        try {
+                            plainPreview[key] = previewData[key];
+                        } catch (e) {}
+                    }
+                    const knownKeys = ['title', 'description', 'canonicalUrl', 'matchedText', 'jpegThumbnail', 'richPreviewType', 'thumbnail', 'doNotLinkify', 'mediaType', 'mediaUrl'];
+                    for (let i = 0; i < knownKeys.length; i++) {
+                        const key = knownKeys[i];
+                        if (previewData[key] !== undefined) {
+                            plainPreview[key] = previewData[key];
+                        }
+                    }
+                    options = { ...options, ...plainPreview };
                 }
             }
         }
@@ -611,9 +625,26 @@ exports.LoadUtils = () => {
                 const preview = await window
                     .require('WAWebLinkPreviewChatAction')
                     .getLinkPreview(link);
-                preview.preview = true;
-                preview.subtype = 'url';
-                options = { ...options, ...preview };
+                if (preview) {
+                    let previewData = preview.data || preview;
+                    let plainPreview = {
+                        preview: true,
+                        subtype: 'url'
+                    };
+                    for (let key in previewData) {
+                        try {
+                            plainPreview[key] = previewData[key];
+                        } catch (e) {}
+                    }
+                    const knownKeys = ['title', 'description', 'canonicalUrl', 'matchedText', 'jpegThumbnail', 'richPreviewType', 'thumbnail', 'doNotLinkify', 'mediaType', 'mediaUrl'];
+                    for (let i = 0; i < knownKeys.length; i++) {
+                        const key = knownKeys[i];
+                        if (previewData[key] !== undefined) {
+                            plainPreview[key] = previewData[key];
+                        }
+                    }
+                    options = { ...options, ...plainPreview };
+                }
             }
         }
 
