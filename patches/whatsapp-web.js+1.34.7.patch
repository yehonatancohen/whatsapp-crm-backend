diff --git a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
index 540078a..0a7b9fa 100644
--- a/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
+++ b/node_modules/whatsapp-web.js/src/util/Injected/Utils.js
@@ -345,11 +345,11 @@ exports.LoadUtils = () => {
                 let preview = await window
                     .require('WAWebLinkPreviewChatAction')
                     .getLinkPreview(link);
-                if (preview && preview.data) {
-                    preview = preview.data;
-                    preview.preview = true;
-                    preview.subtype = 'url';
-                    options = { ...options, ...preview };
+                if (preview) {
+                    let previewData = preview.data || preview;
+                    previewData.preview = true;
+                    previewData.subtype = 'url';
+                    options = { ...options, ...previewData };
                 }
             }
         }
@@ -611,9 +611,12 @@ exports.LoadUtils = () => {
                 const preview = await window
                     .require('WAWebLinkPreviewChatAction')
                     .getLinkPreview(link);
-                preview.preview = true;
-                preview.subtype = 'url';
-                options = { ...options, ...preview };
+                if (preview) {
+                    let previewData = preview.data || preview;
+                    previewData.preview = true;
+                    previewData.subtype = 'url';
+                    options = { ...options, ...previewData };
+                }
             }
         }
 
